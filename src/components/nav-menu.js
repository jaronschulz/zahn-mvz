import React from "react"
import { homedir } from "os";


const menuItems = [
  {
   name: "home",
   slag: "/"
  },
]
const NavMenu = () => {
  return <nav>hello from MENU</nav>
}

export default NavMenu
